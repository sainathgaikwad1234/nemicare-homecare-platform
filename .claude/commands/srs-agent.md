Read and follow the agent defined in agents/srs-agent.md

The user wants to generate a Software Requirements Specification (SRS) document from project documents. The agent should ask for input documents, auto-detect existing MOMs and user stories in the project, and generate a comprehensive SRS.

Additional context or documents: $ARGUMENTS
