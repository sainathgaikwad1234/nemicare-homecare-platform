Read and follow the agent defined in agents/test-data-agent.md

The user needs test data, fixtures, or mocks. Analyze the data models/schemas and generate test data factory functions, fixture files, and mock responses in automation/fixtures/.

Models path or entity to generate data for: $ARGUMENTS
