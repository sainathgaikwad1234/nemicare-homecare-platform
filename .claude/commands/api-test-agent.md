Read and follow the agent defined in agents/api-test-agent.md

The user wants to test backend API endpoints. Analyze the backend code, discover all API endpoints, and write API tests using Playwright request context in automation/tests/api/.

Backend code path or API to test: $ARGUMENTS
