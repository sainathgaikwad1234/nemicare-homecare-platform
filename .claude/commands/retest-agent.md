Read and follow the agent defined in agents/retest-agent.md

The user wants to run a QA retest sweep — validate bug fixes from Jira, verify deployments, retest in browser, update tickets, and send a summary report.

Retest scope or instructions: $ARGUMENTS
